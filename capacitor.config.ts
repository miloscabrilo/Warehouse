import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.warehouse.mcabrilo',
  appName: 'warehouse',
  webDir: 'dist/warehouse',
  bundledWebRuntime: false
};

export default config;
